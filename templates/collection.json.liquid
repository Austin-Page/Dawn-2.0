{% layout none %}

{
	"title": "{{ collection.title }}",
	"description" : "{{ collection.description }}",
	"products" : [
		{% for product in collection.products %}
			{

				"title" : "{{ product.title }}",
				"price_min" :"{{ product.price_min | money_without_trailing_zeros }}",
				"price_max" :"{{ product.price_max | money_without_trailing_zeros }}",
				"varients" : [
						{% for varient in product.varients %}
							{
                              "id" : {{ varient.id }},
							  "title" : "{{variant.title}}",
                              "option1" : "{{ varient.option1 }}",
                              "option2" : "{{ varient.option2 }}",
                              "option3" : "{{ varient.option3 }}"
	
							}
						{% unless forloop.last %},{% endunless %}

					{% endfor %}
				]
			}
		{% unless forloop.last %},{% endunless %}
		{% endfor %}
	]
}